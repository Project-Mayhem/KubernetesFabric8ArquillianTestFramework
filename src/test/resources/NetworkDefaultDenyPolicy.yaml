# Creates a “default” isolation policy for a Namespace by creating a NetworkPolicy
# that selects all pods but does not allow any traffic
# This ensures that even pods that aren’t selected by any other NetworkPolicy will still be isolated.

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny
spec:
  podSelector: