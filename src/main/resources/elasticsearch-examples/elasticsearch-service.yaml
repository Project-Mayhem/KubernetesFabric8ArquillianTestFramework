kind: Service
apiVersion: v1
metadata:
  name: elasticsearch
spec:
  type: NodePort
  selector:
    component: elasticsearch
  ports:
  - name: http
    protocol: TCP
    port: 9200
    targetPort: http